import { useState } from 'react'
import './Tela.css'

function Initial() {
  const [count, setCount] = useState(0)

  return (
    <div>
    <div class="button">
      <button class="button-solid"> 
      </button>
    </div>
    </div>
  )
}

export default App